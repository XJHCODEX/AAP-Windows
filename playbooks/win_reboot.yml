---
- name: Reboot Windows host
  hosts: l2-windows
  gather_facts: false
  tasks:
    - name: Reboot
      ansible.windows.win_reboot:
        pre_reboot_delay: "{{ pre_reboot_delay_sec | default(10) | int }}"
        reboot_timeout: "{{ reboot_timeout_sec | default(600) | int }}"
